<!-- Partial: header (improved accessibility & semantics) -->
<a class="visually-hidden-focusable" href="#main-content">Skip to content</a>
<header class="site-header" role="banner" aria-label="Top banner">
    <div class="bbc-top-bar">
        <div class="top-bar-left">
            <button class="menu-icon" aria-label="Open menu"><i class="fas fa-bars" aria-hidden="true"></i></button>
            <button class="search-icon" aria-label="Open search"><i class="fas fa-search" aria-hidden="true"></i></button>
        </div>
        <div class="top-bar-center">
            <a href="{{ url_for('index') }}" id="bbc-logo" title="BBC Home" aria-label="BBC Home">
                <img src="https://www.pngall.com/wp-content/uploads/15/BBC-Logo-PNG-Image.png" alt="BBC" width="120" height="50" />
            </a>
        </div>
        <div class="top-bar-right" aria-hidden="false">
            <a href="{{ url_for('register') }}" class="register-button" aria-label="Register">Register</a>
            <a href="{{ url_for('login') }}" class="signin-link" aria-label="Sign in">Sign In</a>
        </div>
    </div>

    <nav class="bbc-main-categories" role="navigation" aria-label="Main categories">
        <div class="main-categories-nav">
            <a href="{{ url_for('index') }}" id="cat-home" {% if request.path == url_for('index') %}class="active"{% endif %}>Home</a>
            <a href="{{ url_for('Article') }}" id="cat-Article" {% if request.path == url_for('Article') %}class="active"{% endif %}>Article</a>
            <a href="{{ url_for('weather') }}" id="cat-weather" {% if request.path == url_for('weather') %}class="active"{% endif %}>Weather</a>
            <a href="{{ url_for('comment') }}" id="cat-Comment" {% if request.path == url_for('comment') %}class="active"{% endif %}>Comment</a>
        </div>
    </nav>
</header>

{# skip-link CSS moved to base.html.j2 styles block #}
